case: telecom-case-g-vendor-adapter-impact
level: L2 - impact_analysis
title: "VendorAdapter.normalizeRawMetric 签名变更影响分析"

description: >
  如果给 VendorAdapter.normalizeRawMetric(RawDeviceMetric, DeviceInfo) 增加 VendorContext 参数，
  需要同步检查哪些厂商实现、Registry、MetricCollectorService、测试和文档？

task: >
  如果给 VendorAdapter.normalizeRawMetric(RawDeviceMetric rawMetric, DeviceInfo deviceInfo)
  增加 VendorContext context 参数，需要同步检查哪些厂商实现、Registry、MetricCollectorService、测试和文档？

target_interface:
  name: VendorAdapter
  method: normalizeRawMetric

target_implementations:
  - HuaweiVendorAdapter
  - ZteVendorAdapter
  - FiberHomeVendorAdapter
  - GenericSnmpVendorAdapter

target_callers:
  - MetricCollectorService
  - VendorAdapterRegistry

noise:
  - "VendorAlarmAdapter.normalize 同名干扰"
  - "DeviceMetricNormalizer.normalize 同名干扰"

ground_truth: docs/benchmark-cases/ground-truth/telecom-case-g-ground-truth.yaml
